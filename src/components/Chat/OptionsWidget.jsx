// src/components/Chat/OptionsWidget.js
import React from "react";
import Options from "./Options";

const optionsWidget = (props) => {
  return <Options {...props} />;
};

export default optionsWidget;
